version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  pre_build:
    commands:
      - $(aws ecr get-login --no-include-email --region ap-southeast-2)
  build:
    commands:
      - docker build -t devops-dashboard-server .
      - docker tag devops-dashboard-server:latest 527476858660.dkr.ecr.ap-southeast-2.amazonaws.com/devops-dashboard-server:latest
      # - command
  post_build:
    commands:
      - docker push 527476858660.dkr.ecr.ap-southeast-2.amazonaws.com/devops-dashboard-server:latest
#artifacts:
  #files:
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths